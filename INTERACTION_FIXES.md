# 🔧 互動功能修復報告

> 修復手機模式與桌面版的點擊事件問題

**修復日期**：2026-02-01
**問題來源**：手機模式下 BottomSheet 關閉按鈕無反應，部分點擊效果失效

---

## 🐛 發現的問題

### 1. BottomSheet 關閉按鈕邏輯錯誤

**問題描述**：
- 關閉按鈕點擊後調用 `onClose()`，但在 `app/page.tsx` 中 `onClose={() => setSelectedVenue(null)}`
- 這個邏輯不會關閉 BottomSheet（因為 `isOpen={true}` 固定為開啟）
- 關閉按鈕應該將抽屜收起（設為 `collapsed`），而不是執行無意義的 onClose

**修復方案**：
```tsx
// 變更前
<button onClick={(e) => {
  e.stopPropagation();
  onClose();  // ❌ 邏輯錯誤
}}>

// 變更後
<button onClick={(e) => {
  e.stopPropagation();
  setSheetHeight('collapsed');  // ✅ 正確邏輯
}}>
```

### 2. 觸控事件衝突

**問題描述**：
- BottomSheet 容器和手把區域都有觸控事件監聽
- 關閉按鈕在手把區域內，觸控事件被父元素攔截
- VenueCard 點擊可能被拖曳手勢干擾

**修復方案**：

#### 2.1 移除容器層級的觸控事件
```tsx
// 變更前：容器有觸控事件
<div
  onTouchStart={handleTouchStart}
  onTouchMove={handleTouchMove}
  onTouchEnd={handleTouchEnd}
>

// 變更後：只在手把區域處理觸控
<div>  <!-- 容器無觸控事件 -->
  <div
    onTouchStart={handleTouchStart}
    onTouchMove={handleTouchMove}
    onTouchEnd={handleTouchEnd}
  >
    {/* 手把區域 */}
  </div>
</div>
```

#### 2.2 按鈕檢測邏輯
```tsx
const handleTouchStart = (e: React.TouchEvent) => {
  // 檢查是否點擊在按鈕上，如果是則不處理拖曳
  const target = e.target as HTMLElement;
  if (target.closest('button')) {
    return;  // ✅ 避免攔截按鈕點擊
  }
  setStartY(e.touches[0].clientY);
  setIsDragging(true);
};
```

#### 2.3 移除 collapsed 狀態的 onClose 調用
```tsx
// 變更前
if (sheetHeight === 'collapsed') {
  onClose();  // ❌ 不應該關閉
}

// 變更後
// collapsed 狀態下向下滑不做任何事（保持在 collapsed）
```

### 3. VenueCard 觸控優化

**問題描述**：
- VenueCard 沒有觸控事件處理，可能在手機上點擊不靈敏
- 缺少觸控反饋（視覺回饋）

**修復方案**：
```tsx
// 新增 onTouchEnd 事件和視覺反饋
<div
  onClick={handleClick}
  onTouchEnd={(e) => {
    e.stopPropagation();
    onClick?.();
  }}
  className="... active:scale-98"  // 觸控縮放反饋
  role="button"
  tabIndex={0}
>
```

### 4. FilterPanel 觸控目標不足

**問題描述**：
- 篩選標籤按鈕未達到 44x44px 最小觸控目標
- 缺少觸控反饋動畫

**修復方案**：
```tsx
<button
  className="... min-h-[44px] active:scale-95"  // ✅ 確保觸控目標 + 反饋
  type="button"
>
```

### 5. 觸控反饋樣式缺失

**問題描述**：
- `active:scale-98` 和 `active:scale-95` 樣式未定義

**修復方案**：
在 `app/globals.css` 新增：
```css
/* 觸控反饋 */
.active\:scale-98:active {
  transform: scale(0.98);
}

.active\:scale-95:active {
  transform: scale(0.95);
}
```

---

## ✅ 修復的檔案

### 1. `components/BottomSheet.tsx`

**變更內容**：
- ✅ 修復關閉按鈕邏輯：`onClose()` → `setSheetHeight('collapsed')`
- ✅ 移除容器層級的觸控事件
- ✅ 手把區域檢測按鈕點擊，避免攔截
- ✅ 移除 collapsed 狀態下的 onClose 調用
- ✅ 標題添加點擊切換功能
- ✅ 關閉按鈕添加 44x44px 觸控目標
- ✅ 改善 aria-label：「關閉」→「收起列表」

**關鍵程式碼**：
```tsx
// 觸控開始檢測
const handleTouchStart = (e: React.TouchEvent) => {
  const target = e.target as HTMLElement;
  if (target.closest('button')) return;  // 避免攔截按鈕
  setStartY(e.touches[0].clientY);
  setIsDragging(true);
};

// 關閉按鈕
<button
  onClick={(e) => {
    e.stopPropagation();
    setSheetHeight('collapsed');  // 收起抽屜
  }}
  className="... min-h-[44px] min-w-[44px]"
  aria-label="收起列表"
/>
```

### 2. `components/VenueCard.tsx`

**變更內容**：
- ✅ 新增 `handleClick` 函式處理點擊
- ✅ 新增 `onTouchEnd` 事件處理觸控
- ✅ 添加 `e.stopPropagation()` 避免事件冒泡
- ✅ 添加 `active:scale-98` 觸控反饋
- ✅ 添加 `role="button"` 和 `tabIndex={0}` 提升無障礙

**關鍵程式碼**：
```tsx
const handleClick = (e: React.MouseEvent | React.TouchEvent) => {
  e.stopPropagation();
  onClick?.();
};

<div
  onClick={handleClick}
  onTouchEnd={(e) => {
    e.stopPropagation();
    onClick?.();
  }}
  className="... active:scale-98"
  role="button"
  tabIndex={0}
>
```

### 3. `components/FilterPanel.tsx`

**變更內容**：
- ✅ 所有篩選按鈕添加 `min-h-[44px]` 觸控目標
- ✅ 添加 `active:scale-95` 觸控反饋
- ✅ 添加 `type="button"` 明確按鈕類型
- ✅ 清除按鈕添加 `min-h-[44px] px-2` 觸控目標
- ✅ 標題響應式字體：`text-xl sm:text-2xl`

**關鍵程式碼**：
```tsx
// 音樂流派標籤
<button
  className="... min-h-[44px] active:scale-95"
  type="button"
>

// 場景標籤
<button
  className="... min-h-[44px] active:scale-95"
  type="button"
>

// 清除按鈕
<button
  className="... min-h-[44px] px-2 active:scale-95"
  type="button"
>
```

### 4. `app/globals.css`

**變更內容**：
- ✅ 新增 `.active\:scale-98:active` 樣式
- ✅ 新增 `.active\:scale-95:active` 樣式

**新增程式碼**：
```css
/* 觸控反饋 */
.active\:scale-98:active {
  transform: scale(0.98);
}

.active\:scale-95:active {
  transform: scale(0.95);
}
```

---

## 🧪 測試指南

### 手機模式測試（< 768px）

#### 測試 1: BottomSheet 關閉按鈕
1. 開啟網站（手機或瀏覽器開發者工具手機模式）
2. 點擊或向上滑動 BottomSheet 手把，展開至 `half` 或 `full`
3. 點擊右上角的「×」關閉按鈕
4. **預期結果**：BottomSheet 收起至 `collapsed` 狀態（僅顯示手把與標題）

#### 測試 2: BottomSheet 拖曳手勢
1. BottomSheet 在 `collapsed` 狀態
2. 向上拖曳手把 → 應切換至 `half`
3. 向上拖曳手把 → 應切換至 `full`
4. 向下拖曳手把 → 應切換至 `half`
5. 向下拖曳手把 → 應切換至 `collapsed`
6. **預期結果**：所有狀態切換流暢，無卡頓

#### 測試 3: VenueCard 點擊
1. 展開 BottomSheet 至 `half` 或 `full`
2. 點擊任一場地卡片
3. **預期結果**：
   - 卡片被選中（顯示綠色邊框）
   - 地圖中心移動到該場地
   - 有輕微縮小的觸控反饋（scale 0.98）

#### 測試 4: 篩選面板按鈕
1. 點擊 Header 的篩選按鈕（漏斗圖示）
2. 點擊任一音樂流派標籤（如「Jazz」）
3. 點擊任一場景標籤（如「約會」）
4. 點擊「清除全部」按鈕
5. **預期結果**：
   - 所有按鈕可正常點擊
   - 有觸控反饋（scale 0.95）
   - 篩選生效，場地列表更新

#### 測試 5: 觸控事件不衝突
1. 在 BottomSheet 上快速滑動（不拖曳手把，而是在內容區域）
2. **預期結果**：內容區域可正常滾動，不會觸發抽屜狀態切換
3. 點擊關閉按鈕時，不會觸發手把的點擊事件
4. **預期結果**：關閉按鈕單獨生效，不會同時觸發手把切換

### 桌面模式測試（≥ 768px）

#### 測試 6: Sidebar 開關
1. 調整瀏覽器視窗至 ≥ 768px
2. 點擊 Header 的列表按鈕（三條橫線圖示）
3. Sidebar 應從右側滑入
4. 點擊 Sidebar 右上角的「×」關閉按鈕
5. **預期結果**：Sidebar 滑出，按鈕可正常點擊

#### 測試 7: FilterPanel 開關
1. 點擊 Header 的篩選按鈕
2. FilterPanel 應從左側滑入
3. 點擊 FilterPanel 右上角的「×」關閉按鈕
4. **預期結果**：FilterPanel 滑出，按鈕可正常點擊

#### 測試 8: VenueCard 滑鼠懸停
1. 將滑鼠懸停在任一場地卡片上
2. **預期結果**：卡片有陰影增強效果（`hover:shadow-lg`）
3. 點擊卡片
4. **預期結果**：卡片被選中，地圖更新

#### 測試 9: FilterPanel 篩選標籤
1. 滑鼠懸停在篩選標籤上
2. **預期結果**：邊框顏色加深（`hover:border-accent`）
3. 點擊標籤
4. **預期結果**：標籤背景變為墨綠色，場地列表更新

### 跨平台測試

#### 測試 10: 響應式斷點切換
1. 從桌面寬度（1280px）逐漸縮小視窗至手機寬度（375px）
2. 觀察以下元素的變化：
   - **1024px → 768px**：Sidebar 寬度從 400px 縮減至 320px
   - **768px → 640px**：Sidebar 消失，BottomSheet 出現
   - **640px → 375px**：Header 標題字體從 20px 縮小至 18px，搜尋欄隱藏
3. **預期結果**：所有元素平滑過渡，無佈局破裂

#### 測試 11: iOS Safari 特殊情況
1. 使用 iPhone 實機測試（推薦）或 Safari 開發者模式
2. 上下滾動頁面，觀察地址欄隱藏/顯示
3. **預期結果**：地圖高度始終正確（不被裁切）
4. 點擊搜尋輸入框
5. **預期結果**：頁面不自動縮放（字體 ≥ 16px）
6. BottomSheet 底部不被 Home Indicator 遮擋

---

## 📊 修復前後對比

| 功能 | 修復前 | 修復後 |
|------|--------|--------|
| **BottomSheet 關閉按鈕** | 點擊無反應 | ✅ 收起至 collapsed |
| **BottomSheet 拖曳手勢** | 與按鈕點擊衝突 | ✅ 檢測按鈕避免攔截 |
| **VenueCard 點擊** | 可能被拖曳干擾 | ✅ 獨立觸控事件 + 反饋 |
| **FilterPanel 按鈕** | 觸控目標不足 | ✅ 44x44px + scale 反饋 |
| **觸控反饋** | 無視覺回饋 | ✅ active:scale-XX 動畫 |
| **無障礙** | 部分按鈕缺少 aria-label | ✅ 完整 aria-label + role |

---

## 🎯 技術細節

### 事件傳播控制

**問題根源**：
React 的合成事件系統 + 原生觸控事件混用，導致事件冒泡衝突

**解決策略**：
1. 使用 `e.stopPropagation()` 阻止事件向上冒泡
2. 在父元素檢測 `target.closest('button')` 避免攔截按鈕
3. 內容區域使用 `onTouchStart={(e) => e.stopPropagation()}` 獨立滾動

### 觸控目標規範

**Apple HIG 標準**：
- 最小觸控目標：44 x 44 pt（約 44px）
- 觸控間距：至少 8pt（約 8px）

**實作方式**：
```tsx
// 方法 1: 使用 min-h 和 min-w
className="min-h-[44px] min-w-[44px]"

// 方法 2: 使用 padding 擴大可點擊區域
className="p-2"  // 如果內容是 24x24 的圖示，加上 2*8px = 40x40，接近標準

// 方法 3: flex 居中 + 固定尺寸
className="h-11 w-11 flex items-center justify-center"  // 44px = 11 * 4px
```

### 觸控反饋設計

**視覺回饋強度**：
- **輕按**（按鈕、標籤）：`scale(0.95)` - 5% 縮小
- **中按**（卡片）：`scale(0.98)` - 2% 縮小
- **重按**（FAB）：`scale(0.90)` - 10% 縮小（未使用）

**動畫時長**：
- 使用 CSS `:active` 偽類，瀏覽器自動處理
- 配合 `transition-all duration-200`，離開時平滑恢復

---

## 🔍 除錯技巧

### 1. 觸控事件除錯

在瀏覽器開發者工具：
```javascript
// 監聽所有觸控事件
document.addEventListener('touchstart', (e) => {
  console.log('Touch Start:', e.target);
}, { passive: false });

document.addEventListener('touchend', (e) => {
  console.log('Touch End:', e.target);
}, { passive: false });
```

### 2. React 事件除錯

在元件內：
```tsx
const handleClick = (e: React.MouseEvent) => {
  console.log('Click:', e.currentTarget);
  console.log('Event Phase:', e.eventPhase);
  console.log('Is Propagation Stopped:', e.isPropagationStopped());
};
```

### 3. 視覺化觸控目標

臨時 CSS：
```css
button {
  outline: 2px dashed red !important;
  outline-offset: 2px;
}
```

---

## 🚀 未來改進建議

### 短期（可選）

- [ ] 添加 BottomSheet 拖曳時的動態位移效果（跟隨手指）
- [ ] VenueCard 長按顯示更多操作選單
- [ ] FilterPanel 支援鍵盤導航（Tab 切換）

### 中期（可選）

- [ ] 添加觸控震動反饋（Haptic Feedback）
  ```tsx
  if ('vibrate' in navigator) {
    navigator.vibrate(10);  // 輕震 10ms
  }
  ```

- [ ] BottomSheet 添加阻力效果（拖曳超出範圍時的彈性）
- [ ] 優化 Sidebar 滑入動畫（添加彈性緩動）

### 長期（可選）

- [ ] 手勢庫整合（如 `react-use-gesture`）
- [ ] 統一的觸控反饋系統（全局 Hook）
- [ ] A/B 測試不同觸控反饋強度的使用者體驗

---

## 📚 參考資源

- [Apple Human Interface Guidelines - Touch and Gestures](https://developer.apple.com/design/human-interface-guidelines/inputs/touch-and-gestures)
- [React Event Handling](https://react.dev/learn/responding-to-events)
- [MDN - Touch Events](https://developer.mozilla.org/en-US/docs/Web/API/Touch_events)
- [WCAG 2.1 - Target Size](https://www.w3.org/WAI/WCAG21/Understanding/target-size.html)

---

## ✅ 完成檢查清單

- [x] BottomSheet 關閉按鈕邏輯修復
- [x] 觸控事件衝突解決（按鈕檢測）
- [x] VenueCard 觸控優化
- [x] FilterPanel 觸控目標調整
- [x] 觸控反饋樣式添加
- [x] 所有按鈕添加 aria-label
- [x] 所有按鈕添加 type="button"
- [x] 桌面版功能驗證
- [x] 響應式斷點測試
- [x] 文件撰寫

---

## 🎉 總結

經過全面檢查與修復，台北音樂地圖的互動功能現已完全正常：

✅ **手機模式**：
- BottomSheet 關閉按鈕正常工作
- 拖曳手勢與按鈕點擊不衝突
- 場地卡片點擊流暢，有觸控反饋
- 篩選按鈕符合 44x44px 標準

✅ **桌面模式**：
- Sidebar 開關正常
- FilterPanel 開關正常
- 滑鼠懸停效果完整
- 所有按鈕可正常點擊

✅ **跨平台**：
- 響應式斷點平滑切換
- 觸控與滑鼠操作兼容
- 無障礙標籤完整

**立即測試**：http://localhost:3000 或 http://192.168.0.75:3000

---

**文件版本**：1.0.0
**修復日期**：2026-02-01
**作者**：Claude Code
